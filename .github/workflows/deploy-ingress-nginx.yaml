name: Deploy Ingress-Nginx

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment (prod or stg or qa)'
        required: true
        type: choice
        options:
          - prod
          - stg
          - qa

jobs:
  deploy:
    name: Deploy Ingress-Nginx with Helm
    runs-on: ubuntu-latest

    environment: ${{ inputs.environment }}
#cluster and rg name is started with mycluster
    env:
      CLUSTER_NAME: mycluster-${{ inputs.environment }}-aks
      RESOURCE_GROUP: mycluster-${{ inputs.environment }}-apps-rg
      ENV: ${{ inputs.environment }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Install kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: 'latest'

      - name: Set AKS context
        uses: azure/aks-set-context@v4
        with:
          resource-group: ${{ env.RESOURCE_GROUP }}
          cluster-name: ${{ env.CLUSTER_NAME }}

      - name: Set up Helm
        uses: azure/setup-helm@v4.3.0

      - name: Add Ingress-Nginx Helm repo
        run: |
          helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
          helm repo update

      - name: Install ingress-nginx
        run: |
          helm upgrade --install ingress-nginx ingress-nginx/ingress-nginx \
            --namespace ingress-nginx \
            --create-namespace \
            -f ingress-nginx/${ENV}/values.yaml
